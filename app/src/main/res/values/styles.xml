<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">        <!-- Personnalisez votre thème ici. -->
        <item name="android:textViewStyle">@style/CustomTextView</item>
        <!-- Appliquer le style de bouton personnalisé globalement -->
        <item name="buttonStyle">@style/MyButtonStyle</item>
    </style>

    <!-- Custom TextView style -->
    <style name="CustomTextView" parent="android:Widget.TextView">
        <item name="android:fontFamily">@font/customfont</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Style de bouton personnalisé -->
    <style name="MyButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_background</item>
        <item name="android:textColor">#ffffff</item>
        <item name="android:padding">15dp</item> <!-- Ajouter du padding -->
    </style>
</resources>
